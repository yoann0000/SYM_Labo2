<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.labo2">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:label="@string/app_name" android:name=".ui.AsynchronCommunicationActivity"/>
        <activity android:label="@string/app_name" android:name=".ui.CompressCommunicationActivity"/>
        <activity android:label="@string/app_name" android:name=".ui.DifferCommunicationActivity"/>
        <activity android:label="@string/app_name" android:name=".ui.GraphQLCommunicationActivity"/>
        <activity android:label="@string/app_name" android:name=".ui.ObjectCommunicationActivity"/>
    </application>
</manifest>